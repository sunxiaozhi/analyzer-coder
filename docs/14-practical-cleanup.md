# 实用化清理与验收记录

> 历史清理记录：后续已进一步删除变更审查，当前范围以 `01-requirements.md` 为准。

日期：2026-09-07。

**先用这条主线：登录改密 → 接入仓库 → 准备内容索引 → 搜索代码 → 本地证据问答 → 回看引用。**

每个功能怎么操作、数据从哪里来、哪些链路仍不明确，见 [系统使用说明](13-system-usage-map.md)。

## 删除了什么

删除 21 个未被当前页面或入口引用的遗留文件，约 3,500 行，及对应未使用的前端 API 声明：

| 类别 | 清理内容 |
| --- | --- |
| 旧总览 | 架构图、决策简报、README 阅读、资产/结构/导航面板、模块抽屉和对应 composable |
| 旧图谱页 | GraphView、CallGraphCanvas、callGraphLayout；当前代码证据面板继续保留图谱能力 |
| 旧上下文入口 | AgentContextPackPanel、taskContext 前端客户端 |
| 遗留组件 | CodePreview、EmptyState、RepositorySelector、空权限弹窗、语法修复备份 |
| 配置与评测 | 未引用的 Flyway 迁移占位参数；更新旧路径样本，移除已退役页面的 QA-022，未伪造人审结果 |

当前页面仍调用的服务、API、MCP 和审查集成保留。外部依赖未配置不能等同于代码未实现。

## 修好了什么

- 中英文紧邻的类名问题能拆出正确检索词，精确符号命中不再被无关 README 压过。
- 准备项目时切换仓库、切走再切回或关闭页面，旧请求不会覆盖当前状态或继续推进后续阶段。
- 只读用户能从引用打开知识卡；编辑和草稿工具仍受权限控制。
- 已登录用户可进入项目管理接入自己的仓库；现有仓库操作继续按服务端权限校验。
- 统计加载失败显示“—”和重试提示；搜索提示与实际检索范围一致。

## 实际验收结果

运行命令：`node scripts/verify-core.mjs`。要求已安装前端/MCP 依赖、Java、Maven，并启动 Docker。

| 检查 | 结果 |
| --- | --- |
| 前端 | 36 项通过；TypeScript 检查与生产构建通过 |
| 后端 | 260 项中 258 项通过、2 项 Linux 专用 CodeGraph 测试在 Windows 跳过；无失败 |
| 真实 HTTP + PostgreSQL/pgvector | 登录、首次改密、ZIP 上传、内容索引、检索、源码读取、本地问答、会话引用持久化、匿名拒绝访问通过 |
| 向量数据库回归 | 切换模型后旧向量不混用，搜索不隐式补建，显式重建恢复覆盖通过 |
| MCP | 8 项通过 |
| 评测与 CI 脚本 | 8 项通过；样本结构校验通过，不代表所有样本已完成人审或质量达标 |

脚本每次使用单独的临时数据库容器、随机凭据与受管文件目录，结束后删除临时容器；不读取或改写业务数据库。默认测试端口 15439，可用 `--port=其他端口` 调整。测试日志在本次工作区的 `.runtime/practical-verification.log`。

尚未实测：浏览器整页端到端交互、真实外部模型、真实 CodeGraph CLI、远端 PR/MR、生产部署、历史数据库升级。生产构建仍提示主包较大；不影响本次构建通过，但首屏性能尚未验收。

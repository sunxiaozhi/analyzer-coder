# 真实性与实用价值优化需求

版本：v1.0  
日期：2026-08-25  
状态：执行中

## 1. 目标

将系统从“具备代码知识功能”收敛为“结论边界清楚、证据可追溯、失败不会伪装成成功”的代码证据工作台。

本轮只接受可验证的产品行为：

1. 原始事实必须绑定仓库、快照、提交、路径、行号和内容摘要。
2. 派生结论必须说明算法来源、覆盖范围和未知项。
3. 模型输出不得仅因引用格式正确就标记为事实已被支持。
4. 降级、缺失、过期和未验证状态必须进入接口模型，不能只写日志。
5. 每项需求必须有自动化测试或可重复的验收脚本。

## 2. 优先级与开发顺序

| 编号 | 优先级 | 需求 | 当前状态 |
| --- | --- | --- | --- |
| TRUTH-001 | P0 | 服务端强制当前快照隔离 | 已完成 |
| TRUTH-002 | P0 | 重定义问答证据状态与引用校验 | 已完成 |
| TRUTH-003 | P0 | 变更分析逐条携带快照并拒绝混合版本 | 已完成 |
| RETR-001 | P1 | 区分真实 CodeGraph 与启发式关系通道 | 已完成 |
| RETR-002 | P1 | 明示本地哈希检索能力边界 | 已完成 |
| EVAL-001 | P1 | 建立检索、问答、变更影响金标准评测集 | 已完成 |
| GRAPH-001 | P1 | CodeGraph 构建改为真正后台任务 | 已完成 |
| KNOW-001 | P1 | 区分版本当前与人工核验状态 | 已完成 |
| INDEX-001 | P1 | 完善增量索引的重命名、删除和全量回退 | 待开发 |
| OBS-001 | P2 | 输出检索通道状态、覆盖率与降级原因 | 待开发 |
| TEST-001 | P2 | 增加 PostgreSQL、前端和端到端测试 | 待开发 |
| CLEAN-001 | P2 | 删除未路由 mock 页面并同步产品文档 | 待开发 |

## 3. 详细需求

### TRUTH-001 服务端强制当前快照隔离

问题：仓库发布新快照后、索引完成前，部分查询只按仓库过滤，可能返回旧快照 chunk。

要求：

1. 代码关键词、代码向量、结构关系、chunk 列表、chunk 计数和图谱目标查询必须匹配 `repositories.current_snapshot_id`。
2. 当前快照尚无索引时返回空结果或明确的未准备状态，不得回退到旧快照。
3. 知识卡片代码引用的有效性判断只能使用当前快照 chunk。
4. 历史问答继续保存原引用快照，不受当前快照过滤影响。

验收：构造“仓库当前快照 B、数据库仍保留快照 A chunk”的状态，所有实时查询均不得返回 A；历史记录仍可恢复 A 的引用快照。

### TRUTH-002 重定义问答证据状态

问题：当前 `SUPPORTED` 只证明答案中存在合法 `[S编号]`，不证明事实受到证据支持。

要求：

1. 将格式校验与事实支持拆成不同状态。
2. 每个事实段至少包含合法引用；无引用段落不能进入“有引用”状态。
3. 未做语义蕴含验证时不得使用“已证实”等强语义标签。
4. 接口返回引用覆盖率、非法引用和未引用事实段数量。

验收：仅末尾带一个引用的多段事实回答不能进入最高证据状态。

### TRUTH-003 变更分析版本一致性

要求：

1. 每个候选、测试建议和依赖样例携带自己的 `snapshotId` 与 `contentHash`。
2. 任一代码证据与分析顶部快照不一致时直接排除并记录未知项。
3. HIGH 置信度改名为“证据覆盖等级”，避免解释为统计概率。

### RETR-001 结构检索真实性

问题：问答内部 `CODE_GRAPH` 通道来自字符串 `symbol(` 匹配，不是 CodeGraph CLI。

要求：

1. 该通道改名为 `HEURISTIC_CALL_REFERENCE`，或直接接入已发布 CodeGraph 产物。
2. 启发式关系在索引阶段构建，禁止每次查询全量删除重建。
3. 接口必须返回关系来源、快照和算法限制。

### RETR-002 本地哈希检索边界

要求：所有接口与页面将 `LOCAL_HASH` 展示为“字符相似度”，不得称为模型语义理解；外部 embedding 才显示“语义检索”。

### EVAL-001 质量评测

建立至少三类代表仓库和以下金标准：

- 50 个源码定位问题；
- 30 个带人工证据的问答；
- 20 个已知真实修改范围的变更任务。

发布门槛至少包括 Recall@10、引用覆盖率、陈述支持率、变更文件召回率和 P95 延迟；不得用界面演示代替质量验收。

### GRAPH-001 CodeGraph 后台化

构建接口只入队并立即返回；Worker 执行复制、构建和发布；任务支持心跳、超时、取消和失败原因；旧产物在新产物成功前继续可用。

### KNOW-001 知识状态语义

拆分为来源版本状态、人工评审状态和发布状态。保存卡片不能自动等价于人工核验，自动生成的 Markdown 卡片保持草稿和未评审。

### INDEX-001 增量索引完整性

使用可识别增加、修改、删除、重命名的 Git 差异；基线缺失、差异失败、脏工作区或变更比例过高时自动全量回退，并在任务详情记录实际执行模式。

### OBS-001 可观察性

问答和检索响应返回实际启用通道、不可用通道、模型、快照、召回数量、耗时和降级原因；禁止吞掉异常后仍显示完整能力。

### TEST-001 测试体系

默认 CI 启动 PostgreSQL/pgvector 集成测试；增加真实 MyBatis SQL、快照切换、CodeGraph CLI 契约、前端关键路由和至少一条仓库导入到问答的端到端测试。

### CLEAN-001 清理与文档

删除未路由的静态 mock 页面和废弃空适配器；需求文档以当前路由、接口和数据库行为为准，代码变更必须同步能力状态。

## 4. 完成定义

单项需求只有同时满足以下条件才可标记完成：

1. 产品行为、接口字段和错误语义已实现。
2. 自动化测试覆盖正常、降级和版本切换路径。
3. 相关页面没有扩大后端结论含义。
4. 文档状态与实际代码一致。
5. 后端测试、前端类型检查和生产构建通过。

## 5. 开发记录

### 2026-08-25 · TRUTH-001 完成

- chunk 列表、计数、关键词、向量、图谱目标和内部结构检索统一绑定当前快照。
- 知识引用有效性只认可当前快照中的同内容 chunk。
- 变更影响分析增加应用层版本二次过滤。
- 增加 MyBatis XML 可加载性、SQL 当前快照契约和旧快照证据排除测试。

### 2026-08-25 · TRUTH-002 完成

- 新回答不再使用 `SUPPORTED`；改为 `CITATION_COMPLETE` 或 `CITATION_INCOMPLETE`，只描述机械引用覆盖。
- 引用校验按段落和 Markdown 列表项计算，返回事实段总数、已引用段数、未引用段数、覆盖率和非法引用编号。
- 接口固定返回 `entailmentVerified=false`，页面明确提示尚未验证证据是否在语义上支持回答。
- 越界引用或完全无引用的模型回答继续安全降级，并保留被拒绝输出的引用评估结果。
- 数据库迁移保留历史 `SUPPORTED` 数据可读，历史页面将其明确显示为“历史引用格式校验”。
- 增加完整覆盖、不完整覆盖、非法引用、列表逐项覆盖以及旧 JSON 快照兼容测试。

### 2026-08-25 · TRUTH-003 完成

- 变更候选和现有测试建议逐条返回自己的 `snapshotId` 与 SHA-256 `contentHash`。
- 架构依赖样例由路径字符串升级为带来源路径、关联路径、快照和内容摘要的版本凭据。
- 旧快照代码、旧快照测试、无有效内容摘要、旧架构图和旧依赖样例均直接排除，并进入结构化未知项。
- 架构扫描会核对“列目录”和“逐文件读取”的快照号；分析期间版本切换会终止该架构结果，禁止把新内容标成旧快照。
- 响应字段 `confidence` 改为 `evidenceCoverage`，页面和复制文本统一使用“证据覆盖等级”，不再暗示统计置信概率。
- 增加候选、测试、依赖版本凭据，混合快照排除以及架构扫描竞态测试。

### 2026-08-25 · RETR-001 完成

- 问答内部关系通道由 `CODE_GRAPH` 改为 `HEURISTIC_CALL_REFERENCE`，不再冒充 CodeGraph CLI。
- 启发式关系表重命名为 `heuristic_call_edges`；真实 CodeGraph 仍使用独立的发布产物表和 CLI 查询。
- 符号名加左括号的字符串关系只在代码索引完成阶段重建，问答、图查询和知识发布不再删除重建关系。
- 启发式图和真实 CodeGraph 响应均返回 `relationSource`、`snapshotId`、`algorithm` 与限制说明。
- 关系重建只删除当前快照的启发式边，保留其他快照数据直到仓库清理。
- 增加查询不触发重建、索引阶段构建、通道名称和 SQL 表隔离测试。

### 2026-08-26 · RETR-002 完成

- `LOCAL_HASH` 在接口和页面统一标记为 `CHARACTER_HASH / 字符相似度`，并明确“不理解同义词、业务含义或代码语义”。
- 外部 embedding 才标记为 `SEMANTIC_EMBEDDING / 语义检索`；检索通道分别使用 `*_CHARACTER_SIMILARITY` 与 `*_SEMANTIC`。
- 向量记录持久化 `retrieval_capability`，模型切换后能力类型、模型、维度或内容任一不匹配都会重建，避免按当前配置误标历史向量。
- 检索结果的对外字段由含义过强的 `semanticScore` 改为 `similarityScore + similarityKind`，历史问答 JSON 仍可通过别名恢复。
- 项目准备度、项目概览、当前向量索引和模型设置均展示实际检索能力及限制。
- 增加本地与外部通道分流、能力持久化 SQL 契约和准备度标签测试。

### 2026-08-26 · EVAL-001 完成

- 新增绑定仓库真实文件的 v1 金标准：50 个源码定位问题、30 个带人工审阅证据的问答、20 个已知修改范围任务。
- 数据覆盖 `JAVA_SPRING`、`VUE_TYPESCRIPT`、`OPS_AUTOMATION` 三类代表项目形态，校验器会拒绝不存在、越界或重复的证据路径。
- 新增独立门禁程序计算 Recall@10、引用覆盖率、人工陈述支持率、变更文件召回率和全链路 P95 延迟。
- 门禁要求结果覆盖全部用例；陈述支持率只接受具名、带时间的人工评审，缺失结果不能被当作通过。
- Linux CI 在所有分支校验数据集和门禁逻辑；版本标签发布必须提供 `evaluation/results/release.json` 且通过备案阈值。
- 增加全通过、低质量失败和结果缺项失败三条门禁测试。

### 2026-08-26 · GRAPH-001 完成

- `POST /codegraph/build` 只创建 `QUEUED` 任务并以 HTTP 202 返回，不再在请求线程复制文件或执行 CLI。
- 新增独立 `CodeGraphJobWorker/CodeGraphJobProcessor`，只领取 `CODEGRAPH` 类型任务并执行复制、构建、校验和发布。
- 任务持久化 `heartbeatAt`、`timeoutAt` 和 `failureCode`；Worker 在复制阶段逐文件、CLI 阶段每秒检查心跳、取消和固定总时限。
- Worker 会回收超过截止时间的遗留 RUNNING 任务；取消请求在下一个安全检查点终止进程并进入 CANCELED。
- 构建期间发生快照切换会拒绝发布；产物切换在独立事务中先退役旧产物再插入新产物，失败前旧发布产物保持不变。
- 任务中心新增 CodeGraph 类型、最近心跳、超时截止和稳定失败代码展示。
- 增加入队不执行、Worker 成功、取消、失败、超时回收和原子发布顺序测试。

### 2026-08-26 · KNOW-001 完成

- 知识卡片拆分为 `publicationStatus`、`sourceVersionStatus` 和 `reviewStatus` 三个独立状态，接口同时返回来源核对时间、人工评审人和评审时间。
- 删除“编辑内容即自动确认当前 commit”的数据库触发器；来源当前只表示自动版本核对，不再暗示人工认可内容。
- 新建、编辑和恢复历史修订统一进入 `DRAFT + UNREVIEWED`，内容变更会清除旧评审；CardInput 不再接受发布状态。
- 新增 MANAGE 权限的人工评审与发布接口。人工通过不会自动发布；发布会拒绝未通过评审或来源已过期的卡片。
- 检索和向量索引只使用“已发布 + 人工通过 + 来源未过期”的知识。
- Markdown 自动生成卡片始终保持草稿和未评审，只更新可验证的来源版本状态。
- 页面分别展示发布、人工评审、来源版本三组标签，并提供显式的评审、发布和撤回操作。
- 增加未评审禁止发布、评审与发布分离、来源过期禁止发布及 SQL 状态独立性测试。

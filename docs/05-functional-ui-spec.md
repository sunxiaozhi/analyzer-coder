# 代码智库系统功能与 UI 设计说明

版本：v3.1
日期：2026-07-31
依据：`01-requirements.md` v3.1、当前 Vue 路由与样式
状态：当前运行 UI 基线

> 本文只描述当前用户能够看到和操作的页面。快速开始、开发者入门、备份恢复、多仓检索和流式问答没有当前入口，不纳入现状信息架构。

## 1. 产品信息架构

### 1.1 主导航

普通账号按当前实现显示：

1. 知识问答
2. 知识卡片
3. 调用图谱
4. 源码检索
5. 仓库管理
6. 索引任务

超级管理员在末尾追加：

7. 账号管理
8. 系统设置

登录成功和根路由默认进入 `/ask`。菜单由平台角色控制；仓库内操作由服务端 capabilities 控制。

### 1.2 页面与路由

| 页面 | 路由 | 页面单一任务 |
| --- | --- | --- |
| 登录 | `/login` | 建立会话或完成首次改密 |
| 知识问答 | `/ask` | 对当前仓库提问、恢复历史并核对代码/知识证据 |
| 知识卡片 | `/knowledge` | 维护人工知识、附件、修订和代码引用 |
| 调用图谱 | `/graph` | 构建 CodeGraph 并查看符号影响关系 |
| 源码检索 | `/search` | 浏览当前快照目录和在线预览文件 |
| 仓库管理 | `/repositories` | 接入、维护和治理仓库 |
| 索引任务 | `/indexing` | 观察任务和当前向量索引 |
| 账号管理 | `/accounts` | 管理账号并查看审计 |
| 系统设置 | `/settings` | 备案、检测、启停问答/向量模型 |

## 2. 全局壳层

### 2.1 侧边栏

- 桌面宽度 204px，白色背景，右侧细边框。
- 品牌区高度 58px。
- 菜单图标和文字同排，激活项使用浅蓝背景与蓝色文字。
- 管理员入口仅在 `auth.isAdmin` 为真时生成。
- 小于等于 760px 时侧栏转为顶部横向导航，隐藏文字以保留空间。

### 2.2 顶部栏

- 桌面高度 58px。
- 左侧为“当前仓库”选择器，数据来自当前账号可见仓库。
- 切换仓库调用服务端账号偏好接口；失败保留当前状态并提示错误。
- 右侧显示账号名称、管理员/普通用户和退出按钮。
- 管理页仍显示全局仓库，但管理列表不以它作为筛选条件。

### 2.3 工作区页签

- 位于顶部栏下方，固定高度 44px，与路由内容间距 12px。
- 只记录访问过的业务路由，同一路由名称只保留一个页签。
- 激活页签使用蓝底白字，非激活页签为透明/浅灰 hover。
- 支持单个关闭、中键关闭、关闭其他和关闭全部。
- 页签轨道可横向滚动。
- localStorage 只保存 `name/title/fullPath`，不保存业务正文。
- 关闭全部后回到知识问答。
- 路由内容使用 KeepAlive，最多缓存 12 个页面实例。

## 3. 页面设计

### 3.1 登录

页面采用左右分栏：左侧插画，右侧品牌和表单；移动端隐藏插画。

当前交互：

1. 用户名、密码为必填。
2. 服务端返回 `CAPTCHA_REQUIRED` 后显示算术验证码。
3. 登录失败统一显示服务端安全说明。
4. `mustChangePassword=true` 时在登录页切换到改密表单，只允许改密或退出。
5. 不提供注册、忘记密码和自助救援入口。
6. 登录成功进入 `/ask` 或原 redirect。

### 3.2 知识问答

页面为左侧当前问题与回答、右侧证据轨道两栏；顶部显示唯一的当前仓库范围和准备状态，历史记录由抽屉承载。

左侧：

- 当前会话按轮次展示问题和回答，选中某轮回答时右侧同步展示该轮证据。
- 历史记录按当前账号和仓库持久化，支持恢复、重命名和删除。
- 输入框支持 Enter/按钮发送，busy 时禁止重复发送。
- “新问题”只重置当前页面，不提前创建空记录。
- 当前请求为一次性 JSON，不显示 Token 流式动画和停止按钮。

右侧证据轨道：

- 显示证据总数和“知识与代码联合检索”。
- 证据区分“当前代码”和“团队知识”。
- 代码证据显示文件、行号、正文摘录、通道和分数。
- 支持“查看源码”“调用图谱”；知识证据支持“查看知识”。
- 知识引用可继续展示其关联代码。

回答状态：

- `SUPPORTED`：外部模型回答通过引用校验。
- `DEGRADED`：使用 deterministic-local。
- `MODEL_OUTPUT_REJECTED`：模型回答被拦截并安全降级。
- `INSUFFICIENT`：证据不足，不调用模型。

当前支持多轮上下文；尚无证据篮、问题模式、建议追问、复制带引用 Markdown 和保存知识草稿。

### 3.3 知识卡片

主区为白色容器、顶部标题搜索和“新建卡片”，下方卡片网格。

卡片列表：

- 展示标题、类型、状态、标签、修订和摘要。
- 支持查看、编辑和历史。
- 当前类型为业务规则、技术决策、接口约定、模块说明。

编辑对话框：

- 宽度 920px，桌面最大高度内滚动。
- 字段为标题、类型、Markdown、附件、关联代码、标签和状态。
- Markdown 编辑器提供编写/预览。
- 附件通过文件选择上传，可插入 `knowledge-attachment://{id}`。
- 关联代码选择器支持按路径/符号查询 chunk、查看源码和移除引用。
- 保存新修订后刷新列表。

详情与历史：

- 详情安全渲染 Markdown，并列出附件与代码引用。
- 历史以时间线显示只读修订。
- “恢复为新草稿”先二次确认，再生成更高修订。

当前没有拖拽/粘贴上传、替代文本面板、异步扫描进度、离页未保存拦截和发布权限分层 UI。

### 3.4 调用图谱

页面由顶部工具栏、图画布和右侧影响面板组成。

工具栏：

- 符号输入。
- 深度 1–5。
- 当前 CodeGraph CLI/产物状态。
- 构建或重新构建按钮。
- “影响分析”主按钮。

画布：

- 使用点阵背景和绝对布局节点。
- 中心节点突出显示，边使用 SVG。
- 空结果时给出引导。

影响面板：

- 显示 LOW/MEDIUM/HIGH 风险。
- 列出 source → target 和 relation。
- 显示静态分析限制。

从问答或知识引用进入时，路由携带 symbol、depth=3 和 analyze=1，并自动分析。当前页面不提供上游/下游方向切换、符号候选选择、模块聚合、节点源码详情和表格替代视图。

### 3.5 源码检索

页面名称固定为“源码检索”，不再使用“代码工作台”或“代码检索”。

桌面布局：左侧 240px 文件导航，右侧文件预览。

左侧：

- 从当前仓库已发布快照加载完整文件列表。
- 支持按文件名/路径检索。
- 文件按目录组织并高亮当前项。
- 可消费问答/知识跳转带来的 path、startLine、endLine。

右侧：

- 顶部显示文件名、路径、语言、大小、行数、快照、分支和 commit。
- 源码按行显示并支持行号定位。
- Markdown 文件使用文档预览，不显示原始 Markdown 源码。
- BAT/CMD 注册 batch/DOS 高亮，并兼容后端 GB18030 解码。
- JSON、YAML、XML、SQL、Java、Vue、TypeScript 等使用对应或自动语法高亮。
- 未知文本类型按纯文本展示。

失败状态：

- 无仓库：提示先选择仓库。
- 无已发布快照：提示先完成仓库接入/重扫。
- 文件超限、二进制、编码不可识别或路径非法：显示服务端错误。
- 移动端当前隐藏右侧预览，仅保留文件列表；这是已知体验限制。

### 3.6 仓库管理

页面包含摘要带、搜索/新增工具栏、仓库表格和分页。

接入仓库对话框：

- 来源 Tab：本地 Git、远程 Git、GitLab、ZIP。
- 本地 Git 填服务端路径。
- 远程 Git/GitLab 填 HTTPS Git 地址和可选分支，并可选择已有凭据。
- “管理凭据”弹窗支持新增、编辑、检测通用 Git HTTPS Token 与 GitLab PAT；接口不回显令牌明文。
- 私有仓库必须选择与目标主机匹配的凭据，不接受内嵌用户名或 Token。
- ZIP 使用文件选择。
- 导入期间禁用关闭和重复提交。

仓库表格：

- 展示仓库/OWNER、来源、分支/commit、当前快照、CodeGraph 状态和最近扫描。
- 操作由 capabilities 决定：准备/画像、编辑、重扫、内容索引、CodeGraph、成员与权限、删除。
- 重扫按 changed 返回“已发布最新版本”或“代码版本无变化”。
- CodeGraph 构建返回任务终态后提示成功/失败。
- 删除明确说明只删除平台数据和受管派生数据，不修改本地原目录。

“准备 / 画像”抽屉：

- 顶部准备轨道固定为代码快照、内容索引、向量索引、调用图谱四阶段，状态来自后端，不模拟百分比动画。
- MAINTAIN 可点击开始/继续准备；前端等待内容索引终态后自动推进 CodeGraph，失败保留具体原因和重试入口。
- 项目画像展示文件/大小、chunk/向量、知识、图节点/边、语言构成、一级目录和按约定文件名识别的关键入口。
- READ 账号可查看画像和跨页快捷入口；没有维护权限时不展示准备按钮。

治理对话框：

- 查看成员和候选账号。
- 授予 READ/MAINTAIN/MANAGE。
- 撤权和所有权转移携带 ownershipVersion。
- 转移可指定新仓库名和旧 OWNER 后续权限。

### 3.7 索引任务

页面使用一个白色主容器，顶部内层 Tab 与下方内容连续，不留独立标题空白。

Tab 1“索引任务”：

- 左侧任务表，右侧任务详情。
- 工具栏只保留成功/取消数量和刷新，不重复显示“索引任务”区块标题。
- 表格展示仓库、类型、状态、步骤、开始/结束/创建时间。
- 选择行更新右侧详情。
- RUNNING/QUEUED 支持取消；FAILED/CANCELED 支持重试。
- 页面轮询任务，离开组件后停止定时器。
- 底部分页使用统一 AppPagination。

Tab 2“当前向量索引”：

- 不在页面内部再次切换仓库，直接使用顶部全局仓库。
- 不显示“当前生效向量索引/仅展示当前快照”这类无行动价值的重复说明。
- 汇总展示当前快照、commit、代码 chunk、已向量化、缺失、知识数量、模型、维度和更新时间。
- 明细 Tab 为代码片段/知识卡片。
- 支持关键词、EMBEDDED/MISSING 和 chunk 类型筛选。
- 只展示仓库当前数据，不展示历史索引任务产物。

### 3.8 账号管理

页面主容器内使用“账号管理/审计日志”Tab，Tab 头与内容连续。

账号 Tab：

- 搜索姓名/账号、分页、新增。
- 表格展示账号、角色、状态、仓库权限数、最近登录、IP 和版本。
- 操作包括编辑、启停、解锁、重置密码和审计定位。
- 编辑只修改显示名称和平台角色/启用状态，保存携带版本。
- 重置临时密码弹窗只在本次操作展示返回值。

审计 Tab：

- 延迟加载。
- 展示事件、操作者、目标账号、仓库、结果、IP、requestId 和时间。
- 从账号操作跳转时按账号聚焦。

当前 OWNER 账号不能直接停用，页面需引导先在仓库治理中完成所有权转移；没有批量接管向导。

### 3.9 系统设置

页面当前是模型备案中心，不展示路径、备份、任务等未实现设置菜单。

顶部 Tab：问答模型、向量模型。

问答模型：

- 工具栏左侧为紧凑“数据外发策略”，说明“允许发送检索命中的代码片段”。
- 工具栏右侧为“新增模型”，两者同一行。
- 页面加载只 GET 设置，不调用保存接口、不弹“代码外发策略已更新”。
- 只有用户切换开关时 PUT 设置并提示成功。
- 模型卡展示协议、可用/启用状态、名称、模型、地址、最近成功和输出上限。
- 操作统一位于卡片底部：切换使用、检测、编辑、停用。
- “停用”与其他模型操作同组，不独占卡片下方区域。
- 激活模型禁止编辑；AVAILABLE 才能切换使用。

问答模型对话框：

- 备案名称、协议、地址、模型、API Key。
- 连接/请求超时、最大 Token、Temperature。
- “检测流式能力”只影响连接检测，不代表当前问答已流式输出。
- 保存按钮文案“保存备案”。

向量模型：

- 支持本地哈希和 OpenAI-compatible。
- 固定 64 维。
- 外部模型填写地址、Key 和超时。
- 卡片展示运行方式、维度、数据外发和备案时间。
- 切换前服务端执行固定 embedding 探针。

## 4. 统一交互规则

1. 全局仓库选择只影响问答、知识、图谱、源码检索和当前向量索引；仓库管理列表独立加载。
2. 页面首次进入通过 API 加载，不使用静态 mock 数据。
3. 路由 KeepAlive 可能保留页面局部状态；切仓 watcher 必须清理旧仓库数据并重新请求。
4. capabilities 是仓库动作唯一权威；管理员路由同时由菜单和路由守卫限制。
5. 危险操作使用二次确认，说明影响对象和不可逆部分。
6. 成功提示只由用户动作完成触发；初始化、刷新和轮询不得弹成功 toast。
7. 空状态区分无仓库、无数据、依赖缺失和无权限。
8. API 错误当前按 `code/message/timestamp` 展示，不解析自由文本决定业务状态。
9. Markdown 必须经过 DOMPurify；源码通过文本节点和高亮库输出，不拼接未消毒 HTML。
10. 页面文案采用用户任务名称，不暴露 SQLite、Mapper、HNSW 等实现词，向量详情除外。

## 5. Vue 组件边界

当前组件映射：

| 路由页 | 组合组件/逻辑 | 职责 |
| --- | --- | --- |
| AskView | AskConversationPanel、AnswerEvidencePanel、useAskConversation | 问答编排、消息、证据 |
| KnowledgeView | CardListItem、Editor、Detail、AttachmentList、ReferenceSelector | 卡片列表、编辑、详情和引用 |
| UnifiedIndexJobsView | useIndexJobs、IndexJobTable、UnifiedIndexJobDetail、CurrentVectorIndexPanel | 任务和向量产物 |
| RepositoriesM0View | RepositoryTable、FormDialog、EditDialog、GovernanceDialog、PreparationDrawer | 仓库 CRUD、准备、画像与治理 |
| AccountsView | AccountTable、Dialog、EditDialog、AuditLogPanel | 账号与审计 |
| GraphView | CallGraphCanvas、callGraphLayout | 图数据和画布 |
| ChunksM0View | RepositoryFileTree、RepositoryFilePreview | 文件加载和预览 |
| SystemSettingsView | 当前未充分拆分 | 模型编排、表单、检测和卡片 |

后续维护约束：

- 路由页应保持组合面，API 副作用进入 composable 或 feature 容器。
- Props 只读，子组件通过 typed emits 上报。
- 原始状态最小化，列表/标签/按钮可用性使用 computed。
- watcher 只处理切仓、路由参数、轮询等副作用，并清理计时器/请求。
- `SystemSettingsView` 和 `ChunksM0View` 是优先拆分对象，但拆分不改变当前产品行为。

## 6. 视觉规范

### 6.1 当前令牌

| 角色 | 值 |
| --- | --- |
| 主画布 | `#f5f5f7` |
| 内容/侧栏 | `#ffffff` |
| 主文字 | `#1d1d1f` |
| 次文字 | `#65656c` / `#818188` |
| 主交互 | `#0066cc` |
| 浅蓝选中 | `#eaf3fd` / `#e8f2fc` |
| 成功 | `#16855b` |
| 边框 | `#dedee3` / `#ececef` |
| 深色代码面 | `#262628` |

### 6.2 形状与密度

- 通用表单/按钮圆角 6px。
- 搜索输入约 10px 圆角。
- 卡片和主容器约 7px 圆角。
- 主要内容间距 10–16px，表格行高 54–72px。
- 系统设置模型卡、知识卡片和工作区页签使用当前已有的轻阴影；不再宣称全局“绝不使用阴影”。
- 代码、commit、ID 使用 SFMono-Regular/Consolas。

### 6.3 识别性元素

本产品的视觉识别来自连续证据轨道、深色源码面、蓝色引用定位和全局仓库上下文，不使用营销站大图、装饰渐变或无业务含义的统计卡堆叠。

根 `DESIGN.md` 只作为配色和字体参考；涉及数据表、代码、图谱和管理页面时，以本文为 UI 权威。

## 7. 响应式与可访问性

1. 760px 以下导航横向化，页面改为单栏。
2. 表格和工作区页签允许横向滚动或紧凑显示。
3. 多栏问答、图谱和设置改为单栏；源码预览当前小屏隐藏右侧，是待改进项。
4. 交互控件提供 aria-label/title；页签使用 role=tablist/tab 与 aria-selected。
5. 键盘可关闭工作区页签；对话框使用 Element Plus 焦点管理。
6. 状态不能只依赖颜色，配合中文文字或图标。
7. Markdown 图片应提供 alt；当前附件插入默认从文件名生成 alt，尚无强制编辑面板。

## 8. 当前 UI 已知债务

1. `SystemSettingsView.vue` 和 `ChunksM0View.vue` 体积较大，未达到路由页仅编排的目标。
2. 路由静态导入导致主 JS 包较大，尚未按页面 lazy load。
3. 移动端源码详情隐藏，无法完成完整预览。
4. 问答已有多轮会话历史，但没有流式状态，不应显示停止生成或断线恢复控件。
5. 知识发布权限和附件扫描状态没有达到目标治理模型。
6. 工作区页签与部分知识卡片存在轻阴影，与旧版“无阴影”规范不一致；本文以当前实现为准。
7. 当前没有前端组件测试和浏览器 E2E，视觉回归依赖人工检查。

## 9. 设计稿与维护规则

`docs/images/ui-*-v9.png`、`docs/ui-prototype.html` 与 `docs/ui-login-prototype.html` 均为历史视觉参考，不再作为功能存在性的证据。页面是否交付以当前 router、Controller 和本文为准。

每次 UI 变更必须同时核对：

1. 路由和角色入口。
2. capabilities 与按钮可见性。
3. API 请求是否由用户动作触发。
4. 切仓、KeepAlive、轮询和对话框状态清理。
5. 空、加载、成功、降级、失败和无权限状态。
6. 桌面与 760px 以下布局。
7. `01-requirements.md` 与 `03-design.md` 的对应行为。

# 任务审查评测结果

此目录保存真实环境导出的原始结果和评分报告，不提交伪造的“满分示例”。每次运行至少保留：

- 数据集版本、运行 ID、仓库映射、Base/Head Commit；
- 10 条任务审查的完整结果；
- 8 条知识漂移的完整结果；
- 每条输出路径在对应版本中的存在性检查；
- 执行时间与评测程序生成的 TP/FP/FN 报告。

发布用文件应写入 `evaluation/results/release.json`。只有金标准全部完成具名人工复核且结果数组与数据集 ID 完全一致时，评测程序才会计算是否通过。

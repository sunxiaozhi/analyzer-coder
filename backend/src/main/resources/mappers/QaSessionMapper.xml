<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.analyzercoder.infrastructure.persistence.mapper.QaSessionMapper">
<insert id="insert">INSERT INTO qa_sessions(id,repo_id,account_id,repository_ids,title,created_at,updated_at) VALUES(#{id},#{repoId},#{accountId},#{repositoryIds},#{title},CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)</insert>
<select id="list" resultType="map">SELECT id,repo_id,repository_ids,title,created_at,updated_at FROM qa_sessions WHERE account_id=#{accountId} AND status='ACTIVE' ORDER BY updated_at DESC</select>
<select id="find" resultType="map">SELECT id,repo_id,repository_ids,title,created_at,updated_at FROM qa_sessions WHERE id=#{id} AND account_id=#{accountId} AND status='ACTIVE'</select>
<select id="messages" resultType="map">SELECT id,role,content,citations,conversation_id,created_at FROM qa_messages WHERE session_id=#{id} ORDER BY created_at,id</select>
<insert id="message">INSERT INTO qa_messages(id,session_id,role,content,citations,conversation_id,created_at) VALUES(#{id},#{sessionId},#{role},#{content},CAST(#{citations} AS jsonb),#{conversationId},CURRENT_TIMESTAMP)</insert>
<update id="touch">UPDATE qa_sessions SET updated_at=CURRENT_TIMESTAMP WHERE id=#{id}</update>
<update id="rename">UPDATE qa_sessions SET title=#{title},updated_at=CURRENT_TIMESTAMP WHERE id=#{id} AND account_id=#{accountId}</update>
<delete id="delete">DELETE FROM qa_sessions WHERE id=#{id} AND account_id=#{accountId}</delete>
</mapper>

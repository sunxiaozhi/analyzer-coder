import{request}from'./http';import type{SessionAccount}from'@/types/security';export interface CaptchaChallenge{id:string;prompt:string;expiresAt:string}export const authApi={captcha:(username:string)=>request<CaptchaChallenge>(`/api/auth/captcha?username=${encodeURIComponent(username)}`),login:(username:string,password:string,captchaId?:string,captchaAnswer?:string)=>request<SessionAccount>('/api/auth/login',{method:'POST',body:JSON.stringify({username,password,captchaId,captchaAnswer})}),me:()=>request<SessionAccount>('/api/auth/me'),changePassword:(currentPassword:string,newPassword:string)=>request<SessionAccount>('/api/auth/change-password',{method:'POST',body:JSON.stringify({currentPassword,newPassword})}),logout:()=>request<void>('/api/auth/logout',{method:'POST'})};

import{request}from'./http';import type{AccountRole,AccountSummary,AuditEvent,CreatedAccount}from'@/types/security';export interface AccountInput{username:string;displayName:string;role:AccountRole;temporaryPassword?:string}export interface PermissionView{repositoryId:string;repositoryName:string;permission:'READ'|'MAINTAIN'|'MANAGE'|null}
export const accountsApi={list:()=>request<AccountSummary[]>('/api/accounts'),create:(i:AccountInput)=>request<CreatedAccount>('/api/accounts',{method:'POST',body:JSON.stringify(i)}),update:(id:string,i:Partial<Pick<AccountSummary,'displayName'|'role'>&{enabled:boolean}>)=>request<AccountSummary>(`/api/accounts/${id}`,{method:'PATCH',body:JSON.stringify(i)}),resetPassword:(id:string)=>request<{temporaryPassword:string}>(`/api/accounts/${id}/reset-password`,{method:'POST'}),unlock:(id:string)=>request<void>(`/api/accounts/${id}/unlock`,{method:'POST'}),audit:(limit=200,offset=0)=>request<AuditEvent[]>(`/api/accounts/audit?limit=${limit}&offset=${offset}`),permissions:(id:string)=>request<PermissionView[]>(`/api/accounts/${id}/permissions`),setPermission:(accountId:string,repoId:string,permission:PermissionView['permission'])=>request<void>(`/api/accounts/${accountId}/permissions/${repoId}`,{method:'PUT',body:JSON.stringify({permission})})};

import{request}from'./http';import type{Repository}from'@/types/api';export type ImportSourceType='REMOTE_GIT'|'GITLAB';export const sourceImportsApi={remote:(input:{name:string;url:string;branch?:string;sourceType:ImportSourceType})=>request<Repository>('/api/repository-imports/remote',{method:'POST',body:JSON.stringify(input)}),zip:(name:string,file:File)=>{const body=new FormData();body.set('name',name);body.set('file',file);return request<Repository>('/api/repository-imports/zip',{method:'POST',body})}};

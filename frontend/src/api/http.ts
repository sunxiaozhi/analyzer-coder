import type{ApiErrorResponse}from'@/types/api';const apiBaseUrl=import.meta.env.VITE_API_BASE_URL??'';let csrfToken:string|null=null;export function setCsrfToken(t:string|null){csrfToken=t}export class ApiError extends Error{readonly status:number;readonly code:string;constructor(status:number,code:string,message:string){super(message);this.name='ApiError';this.status=status;this.code=code}}
export async function request<T>(path:string,init:RequestInit={}):Promise<T>{const method=(init.method??'GET').toUpperCase(),headers=new Headers(init.headers);if(init.body!==undefined&&!(init.body instanceof FormData))headers.set('Content-Type','application/json');if(!['GET','HEAD','OPTIONS'].includes(method)&&csrfToken)headers.set('X-CSRF-Token',csrfToken);const response=await fetch(`${apiBaseUrl}${path}`,{...init,credentials:'include',headers});if(!response.ok){const payload=await readJson<ApiErrorResponse>(response);if(response.status===401&&path!=='/api/auth/login')window.dispatchEvent(new Event('auth-expired'));throw new ApiError(response.status,payload?.code??'HTTP_ERROR',payload?.message??`${response.status} ${response.statusText}`)}if(response.status===204||response.headers.get('content-length')==='0')return undefined as T;return response.json()as Promise<T>}async function readJson<T>(r:Response):Promise<T|null>{try{return await r.json()as T}catch{return null}}
